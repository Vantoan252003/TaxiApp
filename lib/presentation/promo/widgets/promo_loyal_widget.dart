import 'package:flutter/material.dart';

class PromoLoyalWidget extends StatelessWidget {
  const PromoLoyalWidget({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Center(child: Text('Đổi điểm')), 
    );
  }
}
